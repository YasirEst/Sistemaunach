# ─── SEGURIDAD ────────────────────────────────────────────────────────────────
# Genera una clave segura con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=cambia_esto_por_una_clave_segura_generada
ALGORITHM=HS256

# ─── CREDENCIALES ─────────────────────────────────────────────────────────────
ADMIN_USER=admin_unach
ADMIN_PASS=cambia_esto_por_contraseña_segura

# Contraseña genérica temporal para docentes (reemplazar con validación en BD)
DOCENTE_PASS=cambia_esto_tambien

# ─── CORS ─────────────────────────────────────────────────────────────────────
# Separados por coma, sin espacios
# Agrega aquí el dominio real de Vercel de Ximena cuando lo tengas
ALLOWED_ORIGINS=http://localhost:5173,https://sistema-unach-frontend.vercel.app